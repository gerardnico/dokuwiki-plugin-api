language: php
php:
  - "7.2"

env:
  - DOKUWIKI=stable

before_install:
  - wget https://raw.github.com/splitbrain/dokuwiki-travis/master/travis.sh

install:
  - sh travis.sh

script:
  - cd _test && phpunit --stderr --group plugin_restapi --verbose --debug

notifications:
  email:
    - gerardnico@gmail.com
